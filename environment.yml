name: base
channels:
  - defaults
dependencies:
  - conda
  - python=3.7
  - numpy
  - conda-build
  - pyyaml
  - ipython
  - pytorch
  - torchtext
  - torchvision
  - cudatoolkit=11.0
prefix: /opt/conda
